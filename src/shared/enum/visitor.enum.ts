export enum VisitorType {
  VISITOR = 'visitor',
  SERVICE = 'service',
  DELIVERY = 'delivery',
  VENDOR = 'vendor',
  OTHER = 'other',
}

export enum VisitorStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  DENIED = 'denied',
}
